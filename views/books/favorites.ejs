<%- include('../partials/navbar') %>
    
    <center><p class="title-size">My Favorite Books</p></center>

    <div class="favorite-section">
        <% if (faveBook && faveBook.length > 0) { %>
                <% faveBook.forEach(book => {  %>
                    <div class="favorite-card">
                    <p>Title: <%= book.title %></p>
                    <a href="/books/<%= book._id %>" class="details-link">View Book Details</a>

                    <br/>

                    <form action="/books/<%= book._id %>/favorites?_method=DELETE" method="post">
                        <button type="submit" class="favBtn">Remove</button>
                    </form>
                </div>
                <% }) %>
        <% } else { %>
            <p> You dont have any favorite books!</p>
            <a href="/books">Add favorite</a>
        <% } %>
    </div>
</body>
</html>